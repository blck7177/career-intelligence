You are analyzing ALL of this candidate's bullets for ONE experience at once, before any interview begins. Identify the distinct workstreams within this experience — pieces of work that each have their own reason for existing (their own business purpose, deliverable, or audience) — and assign every bullet to exactly one workstream.

A workstream boundary is about WHY the work existed, not what technique was used or what phase it was in. Bullets describing the same underlying deliverable from different angles (build it, then validate it, then report on it) belong in ONE workstream. Only split into separate workstreams when the bullets clearly serve different purposes or deliverables — for example, "ongoing regulatory model review" and "a one-off systems-integration project" at the same employer are genuinely different workstreams, even though both happened at the same company.

Most experiences are a single workstream. Only split when the bullets themselves give real evidence of more than one distinct piece of work — do not manufacture splits for variety, and do not force clearly unrelated bullets into one workstream just to minimize the count.

For each workstream, write situation_and_task: 2-4 sentences on why this work existed — the business/organizational need it served, who it was for, what the deliverable likely was. Use candidate_context (what's established from earlier chronological experiences) to calibrate this if relevant. This frame is what every question about these bullets will be anchored to, so it needs to be concrete, not generic filler that could describe any team's work.

For each workstream, also propose dimensions: 3-6 distinct investigation angles that, together, cover everything in this workstream's bullets worth a thorough interviewer's attention — for example: what the target/deliverable precisely was, the concrete method or mechanism behind a claimed result, data/tooling specifics, ownership/scope boundary, how it was validated or benchmarked. Each dimension needs a short stable key (snake_case) and a one-sentence description. Keep dimensions genuinely distinct from each other — if two would get asked about with the same underlying question, they're one dimension, not two. These keys are permanent for this workstream: every later round of interviewing references them by this exact string.

Every bullet index must appear in exactly one workstream's bullet_indices.
