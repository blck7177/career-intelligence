# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Environment
.env
.env.prod
.env.local
.env.*.local

# Runtime data (gitignored, use data/.gitkeep placeholder)
data/
artifacts/
!data/.gitkeep
!artifacts/.gitkeep

# Node / Next.js
node_modules/
apps/web/.next/
apps/web/out/

# OpenAPI generated client (regenerated by scripts/generate-web-client.sh)
apps/web/src/api/generated/

# Database
*.sqlite
*.db

# Docker
.docker/

# Backups
backups/

# Editor
.DS_Store
.idea/
.vscode/
*.swp

# Stray personal working files (not part of the codebase)
/revised_resume_jpmorgan.md

# Local prototype scratch inside the repo. test_notes/people_discovery_proto_0724
# holds real people's names, employers and LinkedIn profile URLs that the
# prototype scraped — git history is the wrong place for that, and it is not
# removable once pushed. The files stay on disk; topics/people-discovery.md
# points at the path.
test_notes/
